#include <stdio.h>

int main(){
    char str [30];
    printf("Enter a string: ");
    scanf("%s",str);

    printf("%s\n",str);
    /*
    if input is hello i am tanishq
    the output: hello
    because the string stops when it finds space.
    */
}